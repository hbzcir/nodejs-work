function (err){
        if(err){
            console.log(err);
            res.status(500).send('interal server error');
        }
        console.log(req.params.id)
        //res.render(write);
    }